# Your server basics
PORT=8787
WEB_ORIGIN=http://localhost:5173

# === MCP endpoints ===
# REST base (Blockscout MCP REST). If using docker proxy on 8890:
MCP_REST_BASE=http://localhost:8000

# Legacy HTTP tool endpoint (keep if also running the proxy stream)
# MCP_HTTP_URL=http://localhost:8890

# Optional: choose an LLM to summarize MCP tool JSON (ollama | anthropic | none)
LLM_PROVIDER=ollama
OLLAMA_HOST=http://localhost:11434
OLLAMA_MODEL=llama3.1:8b

# If using Anthropic as planner instead
# LLM_PROVIDER=anthropic
# ANTHROPIC_API_KEY=sk-ant-...
# ANTHROPIC_MODEL=claude-3-7-sonnet-latest

# Hedera placeholders
HEDERA_NETWORK=testnet
HEDERA_RPC_URL=https://testnet.hashio.io/api
HEDERA_ACCOUNT_ID=0.0.xxxxxx
HEDERA_PRIVATE_KEY=302e02...
HEDERA_HCS_TOPIC_ID=0.0.yyyyyy

# Pyth (optional)
PYTH_PRICE_ID_USD=
PYTH_ENDPOINT=
